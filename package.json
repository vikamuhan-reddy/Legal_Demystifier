{
  "name": "legal-document-demystifier",
  "version": "2.0.0",
  "private": true,
  "description": "An AI-powered tool to simplify complex legal documents, deployable on Firebase.",
  "scripts": {
    "dev": "firebase emulators:start",
    "deploy": "firebase deploy",
    "postinstall": "cd firebase/functions && npm install"
  },
  "dependencies": {
    "firebase": "^10.12.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "firebase-tools": "^13.11.2",
    "typescript": "^5.4.5"
  }
}
